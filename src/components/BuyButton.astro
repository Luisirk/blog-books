---
import CDL from "./CDL.astro"

const SPAIN = 'ES'

const country = Astro.request.headers.get('X-Vercel-IP-Country') ?? 'ES'

const storeCountry = country === SPAIN ? 'Spain' : 'UK'
const countryName = country === SPAIN ? 'España' : 'United Kingdom'

const { buy } = Astro.props
const url = buy[storeCountry]
---

<a
  href={url}
  title="Comprar libro"
  target="_blank"
  rel="noopener noreferrer"
  class="text-balance inline-flex gap-2 items-center bg-green-900 hover:bg-green-700 active:bg-yellow-600 text-black font-bold py-2 px-4 rounded-lg border border-green-500 transition duration-200 ease-in-out hover:scale-105 justify-center text-center"
>
  Comprar en Casa del Libro {countryName}
  <CDL />
</a>